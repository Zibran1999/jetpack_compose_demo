<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".notesapp.NoteApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackComposeDemo"
        tools:targetApi="31">
        <activity
            android:name=".notesapp.ui.NoteActivity"
            android:exported="true"
            android:theme="@style/Theme.JetpackComposeDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity2"
            android:exported="true"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.JetpackComposeDemo"/>

        <activity
            android:name=".coroutineflow.CoroutineFlowActivity"
            android:exported="true"
            android:label="@string/title_activity_coroutine_flow"
            android:theme="@style/Theme.JetpackComposeDemo"/>
        <activity
            android:name=".InfoActivity"
            android:exported="false"
            android:label="@string/title_activity_info"
            android:theme="@style/Theme.JetpackComposeDemo" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetpackComposeDemo" />
    </application>

</manifest>